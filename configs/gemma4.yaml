# Gemma 4 12B Configuration
# Based on official Unsloth notebook: https://colab.research.google.com/github/unslothai/notebooks/blob/main/nb/Gemma4_(E2B)-Vision.ipynb
model:
  name: unsloth/gemma-4-12b-it
  target_modules: all-linear
  r: 8
  lora_alpha: 8
  lora_dropout: 0
  bias: none
  max_seq_length: 4096
  batch_size: 1
  load_in_4bit: false  # Gemma 4 uses 16-bit LoRA, not 4-bit QLoRA
  use_gradient_checkpointing: unsloth

training:
  num_train_epochs: 3
  learning_rate: 2e-4
  warmup_steps: 10
  gradient_accumulation_steps: 4
  optim: adamw_8bit
  seed: 3407
